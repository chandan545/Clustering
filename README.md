# Clustering
Clustering in machine learning is an unsupervised learning technique that groups similar data points together into clusters, based on their features, without prior labels. The goal is to find natural patterns or structures in the data.
